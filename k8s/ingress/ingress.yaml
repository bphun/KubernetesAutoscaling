apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-ingress
spec:
  ingressClassName: nginx 
  tls:
  - hosts:
    - 192.168.1.52
  rules:
  - host: 192.168.1.52
    http:
      paths:
      - path: /
        backend:
          serviceName: nginx
          servicePort: 80